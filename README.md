# Train ISP API
This is the API for the Train ISP Check project. It provides endpoints for checking the ISP and providing an ICS proxy.

## API Endpoints
- `GET /api/check-isp?ip=<IP_ADDRESS>`: Check the ISP of the given IP address.
- `GET /api/check-google`: Check the connectivity to Google services.